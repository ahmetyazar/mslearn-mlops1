name: Pull request

on:
  pull_request:

jobs:
  job1:
    name: job-placeholder
    runs-on: ubuntu-latest
    steps:
    - name: Placeholder
      run: echo "Will add code checks here in next challenge"
